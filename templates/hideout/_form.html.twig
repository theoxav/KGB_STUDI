{{form_start(form)}}
  
    {{form_row(form.code)}} 
    <div class="row">
      <div class="col-md-6">
        {{form_row(form.country)}} 
      </div>
      <div class="col-md-6">
       {{form_row(form.city)}} 
      </div>
    </div>
    {{form_row(form.address)}} 
    {{form_row(form.type)}} 
     <input type="submit" value="{{ submitButtonText|default('Create Hideout')}}" class="mt-4 btn btn-info w-100" formnovalidate />
       
{{form_end(form)}}