{{form_start(form)}}
  
    {{form_row(form.name)}} 
     <input type="submit" value="{{ submitButtonText|default('Create Hideout Type')}}" class="mt-4 btn btn-info w-100" formnovalidate />
       
{{form_end(form)}}