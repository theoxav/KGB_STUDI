{{form_start(form)}}
  <div class="row">
    <div class="col-md-6 mx-auto">
    {{form_row(form.firstName)}} 
    </div>
     <div class="col-md-6">
    {{form_row(form.lastName)}} 
    </div>
  </div>
  {{form_rest(form)}}
     <input type="submit" value="{{ submitButtonText|default('Create Target')}}" class="mt-4 btn btn-danger w-100" formnovalidate />
       
   {{form_end(form)}}