{% extends "layouts/base.html.twig" %}
{% block title 'Agents' %}

{% block body %}
  <div class="container text-center">
   <div class="col-12 mx-auto">
    <h1 class="mt-4 text-center">Our Agents</h1>

    <table class="mt-5 table table-hover text-center">
    <thead>
        <tr class="table-secondary">
        <th scope="col">FirstName</th>
        <th scope="col">LastName</th>
        <th scope="col">Infos</th>
        </tr>
    </thead>
    <tbody>
    {% for agent in agents %}
        <tr class="table-dark">
        <td>{{agent.firstName}}</td>
        <td>{{agent.lastName}}</td>
        <td><a href="{{ path('app_agent_show', {id: agent.id})}}"<i class="btn btn-info fa-solid fa-plus rounded"></i></a></td>
        </tr>
    {% endfor %}
    </tbody>
       
    </table>
      <a href="{{ path('app_agent_create')}}" class="btn rounded btn-info">Add Agent</a></td>
   </div>
  </div>
{% endblock %}
